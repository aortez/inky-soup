# Yocto upstream sources (cloned by init.sh).
# Commit our custom meta-inky-soup layer, ignore the rest.
src/poky/
src/meta-raspberrypi/
src/meta-openembedded/
src/openembedded-core/

# Build directory - commit conf/, ignore artifacts.
build/poky/*
!build/poky/conf/

# Shared download and sstate caches.
downloads/
sstate-cache/

# Node modules.
node_modules/

# Flash config (contains SSH key path - user-specific).
.flash-config.json
