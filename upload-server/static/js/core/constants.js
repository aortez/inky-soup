/**
 * Application constants.
 */

// Flash job duration in milliseconds.
export const FLASH_DURATION_MS = 40000;

// Cache image dimensions (for preview and dithering).
export const CACHE_WIDTH = 600;
export const CACHE_HEIGHT = 448;

// Thumbnail dimensions (for gallery display).
export const THUMB_WIDTH = 150;
export const THUMB_HEIGHT = 112;

// Default resize filter for new uploads.
export const DEFAULT_FILTER = 'bicubic';
