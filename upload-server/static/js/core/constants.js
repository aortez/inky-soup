/**
 * Application constants.
 *
 * Note: Display dimensions (width, height) are managed in state.js via displayConfig,
 * allowing runtime configuration for different Inky Impression models.
 */

// Flash job duration in milliseconds.
export const FLASH_DURATION_MS = 40000;

// Default resize filter for new uploads.
export const DEFAULT_FILTER = 'bicubic';
